
# This file cannot be run directly, it must be used with the "source" command.
# Even though this file can be added to a version control system, sensitive
# variables, if any, should be declared in a separate file and not be committed
# to version control.

# Set the Current project's root directory:
PROJECT_ROOT_DIR="/home/mo/projects/pyblog"
  
# Activate the virtual environment for the current project:
source "${PROJECT_ROOT_DIR}/venv/bin/activate"   

# Change prompt:
PS1="${PS1/venv/$(basename ${PROJECT_ROOT_DIR}): Development}"

# Set user-defined development environment variables. Each defined variable
# should be unset in the "deactivate" function defined in "venv/bin/activate".
# 
# deactivate () {
#   ...
#   unset VARIABLE 
# }

## Non-sensitive variables:
export FLASK_APP=run_app
export FLASK_DEBUG=True

## Sensitive variables:
export $(cat "${PROJECT_ROOT_DIR}/.env_development") 
